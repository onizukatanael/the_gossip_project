<!DOCTYPE html>
<html>
  <head>
    <link rel="stylesheet" href="https://cdn.rawgit.com/yegor256/tacit/gh-pages/tacit-css-1.2.8.min.css"/>
    <h1>Bienvenue sur The Gossip Project</h1>
    <%= link_to 'Créer un gossip', new_gossip_path %>
  </head>
  <table>
  <tr>
    <th>Auteur anonyme</th>
    <th>Gossip</th>
  </tr>

  <% @gossips.each do |gossip| %>
    <tr>
      <td><%= gossip.anonymous_author %></td>
      <td><%= gossip.content %></td>
      <td><%= link_to 'Montre moi', gossip_path(gossip) %></td>
      <td><%= link_to 'Editer', edit_gossip_path(gossip) %></td>
      <td><%= link_to 'Clique pour détruire', gossip_path(gossip), method: :delete %></td>
    </tr>
  <% end %>
</table>
</html>
